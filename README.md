# Nipoppy Custom Pipeline Integration Example

This repository demonstrates how to integrate a new custom MRI processing pipeline into the Nipoppy framework. It will guide users through environment setup, data standardization to BIDS, containerizing the pipeline, and integrating the pipeline with Nipoppy's workflow.
